{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "functions": {
    "api/index.js": {
      "//": "Картинки и звуки персонажей сервер читает с диска сам, поэтому папку public нужно явно положить рядом с функцией — иначе аватары и index.html не найдутся.",
      "includeFiles": "public/**"
    }
  },

  "//": "Все адреса ведём в одну функцию: маршрутизацией занимается server.js, как и при локальном запуске. Так поведение на сайте и на компьютере совпадает.",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
